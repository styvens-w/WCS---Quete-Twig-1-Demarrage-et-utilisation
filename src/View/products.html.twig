{% extends 'layout.html.twig' %}

{% block title %}
    {{ parent() }} - Produits
{% endblock %}

{% block content %}
    <ul>
        {% for product in products %}
            <li>{{ product }}</li>
        {% endfor %}
    </ul>

    <div>
        {{ dump(products) }}
    </div>
{% endblock %}

